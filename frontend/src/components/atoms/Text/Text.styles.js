import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  text: {
    alignItems: 'center',
    textAlign: 'center',
    color: 'black',
    fontSize: 20,
    marginBottom: 5,
    padding: 5,
  },
})
