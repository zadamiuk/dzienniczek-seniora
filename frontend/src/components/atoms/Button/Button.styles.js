import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  root: {
    alignItems: 'center',
    width: 250,
    marginTop: 30,
    backgroundColor: '#1AA7EC',
    borderRadius: 5,
    padding: 20,
  },
  button: {
    fontWeight: '400',
    fontSize: 20,
    color: 'white',
    textAlign: 'center',
  },
})
