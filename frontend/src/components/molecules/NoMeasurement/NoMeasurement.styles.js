import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    view: { 
        flex: 1, 
        alignItems: 'center', 
    },
    none: {
        width: '100%',
        fontSize: 20,
        textAlign: 'center',
        padding: 20
    }
});