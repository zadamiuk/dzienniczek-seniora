import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  root: {
    marginLeft: 50,
    marginRight: 50,
  },
  label: {
    fontSize: 20,
    textAlign: 'left',
    marginTop: 15,
    marginBottom: 10,
    color: 'black',
  },
  input: {
    fontSize: 18,
    backgroundColor: 'white',
    maxHeight: 70,
    minWidth: 300,
  },
})
