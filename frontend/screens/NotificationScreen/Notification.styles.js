import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    root: {
        alignItems: 'center',
        paddingTop: 50
    },
    image: {
        height: 300,
        width: 300,
    },
    helper: {
        textAlign: 'center',
        fontSize: 20
    }
});